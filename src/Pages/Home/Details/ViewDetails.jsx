import React from 'react';
import useTitle from '../../../Hook/UseTitle';

const ViewDetails = () => {
    useTitle('Details')
    return (
        <div>
            
            Toy details
        </div>
    );
};

export default ViewDetails;